import type { Metadata } from "next";
import { Manrope } from "next/font/google";
import "./globals.css";

const manrope = Manrope({
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Pearl Solutions Group",
  description: "Pearl Solutions provides powerful automation and management tools designed to optimize your online purchasing and payment workflows.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${manrope.className} antialiased dark`}
      >
        {children}
      </body>
    </html>
  );
}
